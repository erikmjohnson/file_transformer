'use strict';

const os = require('os');
const fs = require('fs');


// const peopleNames = ['Erik', 'Nikki', 'Karl', 'Jess', 'Rick'];
//
// const buffedOut = (array) => {
//   array.forEach(x => {
//     for (let j = 0; j < x.length; j++) {
//       console.log(x.charCodeAt(j));
//     }
//   })
// };

// buffedOut(peopleNames);

fs.writeFile(`${__dirname}/loop.js`, (error, buffer) => {

})

